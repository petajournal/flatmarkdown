# flatmarkdown body AST 仕様書

基本仕様は、flatmarkdown仕様書（https://github.com/petajournal/flatmarkdown-spec/）に基づく。

`body_to_ast(input)` は Flat Markdown の項目の本文（body）を解析し、JSON 形式の AST 文字列を返す。

## 前処理

`body_to_ast` はパース前に入力に対して次の変換を行う。

**空白のみの行の正規化**: code fence の外側にある、Unicode のWhite_Spaceプロパティを持つ文字のみで構成される行（空行・スペースのみの行・全角スペースのみの行など）は、すべて `<br />` に置換される。これにより、視覚的に空白に見える行がMarkdownパーサーに渡る前に明示的なハードブレークマーカーへと変換される。（Flat Markdown 仕様書「空行の扱い」を参照。）

**Markdownリンク → wikilink / hashtag 変換**（*resolve_links = true のときのみ*）: パース後、URL が `../ページパス.md`（または `../ページパス.md#id`）のパターンに一致する `link` ノードは次のように変換される。

- リンクテキストが `#ページパス` という値を持つ単一の text ノードである場合（すなわち `[#タグ名](../タグ名.md)`）、そのノードは `hashtag` ノードへ変換され、`value = タグ名` が設定される。`url`・`title`・`children` フィールドは除去される。
- それ以外の場合、ノードは `wikilink` ノードへ変換される。`url` は `../` プレフィックスと `.md` サフィックスを除去した形になり、`#id` フラグメントはそのまま保持される。`title` フィールドは除去され、`children`（リンクテキスト）はラベルとして保持される。

パターンに一致しないリンク（外部URL・`.md`以外の相対パスなど）は `link` ノードのまま保持される。

*resolve_links = false* の場合、この変換は行われない。テキスト中の `#タグ` 抽出も同様にスキップされる。リンク解決を無効にするには `body_to_ast_opts(input, false)` を使用する。

## ノード構造

すべてのノードは最低限 `type` フィールドを持つ JSON オブジェクトである。子要素を持つノードには `children` 配列が含まれる。ノード固有の属性は同じオブジェクトにフラットに格納される。

```json
{
  "type": "<ノードタイプ>",
  "<属性>": "<値>",
  "children": [ ... ]
}
```

- `type` (string) — 常に存在
- `children` (array) — 子ノードが1つ以上ある場合のみ存在

## ノードタイプ一覧

### ブロックノード

#### `document`

ルートノード。AST の最上位に必ず1つ存在する。

#### `paragraph`

段落ブロック。子要素はインラインノード。

#### `heading`

見出し。

| 属性     | 型      | 説明                 |
|----------|---------|----------------------|
| `level`  | integer | 見出しレベル（1〜6） |

#### `code_block`

フェンスコードブロック。子要素は持たず、内容は `literal` に格納される。Flatmarkdown はインデントコードブロックには対応しない。

| 属性      | 型      | 説明                                       |
|-----------|---------|--------------------------------------------|
| `info`    | string  | 開始フェンス後の情報文字列（例: `"rust"`） |
| `literal` | string  | コード内容                                 |

注: 情報文字列が未指定の場合、`info` はデフォルトで `"text"` となる（`default_info_string` による設定）。

#### `block_quote`

`>` による引用ブロック。子要素はブロックノード。

#### `list`

リスト。

| 属性        | 型      | 説明                                       |
|-------------|---------|-------------------------------------------|
| `list_type` | string  | `"bullet"`（箇条書き）または `"ordered"`（番号付き） |
| `start`     | integer | 開始番号（番号付きリストの場合）           |
| `tight`     | boolean | タイトリストなら `true`（`<p>` で囲まない）|
| `delimiter` | string  | `"period"`（`.`）または `"paren"`（`)`）   |

#### `item`

リスト項目。

| 属性        | 型      | 説明                                     |
|-------------|---------|------------------------------------------|
| `list_type` | string  | `"bullet"` または `"ordered"`            |
| `start`     | integer | この項目の序数                           |
| `tight`     | boolean | 親リストがタイトなら `true`              |

#### `task_item`

タスクリスト項目（チェックボックス）。

| 属性     | 型             | 説明                                                      |
|----------|----------------|-----------------------------------------------------------|
| `symbol` | string \| null | 括弧内の文字（例: `"x"`）。未チェックの場合は `null`      |

#### `table`

テーブル。

| 属性          | 型       | 説明                                                     |
|---------------|----------|----------------------------------------------------------|
| `alignments`  | string[] | 列ごとの配置: `"none"`, `"left"`, `"center"`, `"right"` |
| `num_columns` | integer  | 列数                                                     |
| `num_rows`    | integer  | 行数                                                     |

#### `table_row`

テーブル行。

| 属性     | 型      | 説明                           |
|----------|---------|--------------------------------|
| `header` | boolean | ヘッダー行なら `true`          |

#### `table_cell`

テーブルセル。子要素はインラインノード。

#### `thematic_break`

水平線（`---`, `***`, `___`）。属性・子要素なし。

#### `html_block`

生の HTML ブロック。

| 属性         | 型      | 説明                   |
|--------------|---------|------------------------|
| `block_type` | integer | HTML ブロックタイプ（1〜7） |
| `literal`    | string  | 生の HTML 内容         |

#### `footnote_definition`

脚注定義。

| 属性   | 型     | 説明         |
|--------|--------|--------------|
| `name` | string | 脚注ラベル   |

#### `alert`

GitHub スタイルのアラート（例: `> [!NOTE]`）。

| 属性         | 型             | 説明                                                        |
|--------------|----------------|-------------------------------------------------------------|
| `alert_type` | string         | `"note"`, `"tip"`, `"important"`, `"warning"`, `"caution"` |
| `title`      | string \| null | カスタムタイトル。デフォルトの場合は `null`                  |

#### `subtext`

ブロックレベルの下付きテキスト（`<sub>` ブロック）。

### インラインノード

#### `text`

テキスト内容。

| 属性    | 型     | 説明           |
|---------|--------|----------------|
| `value` | string | テキスト内容   |

#### `linebreak`

ハード改行。Flatmarkdownでは、すべての改行コードが必ず`linebreak`として扱われる。GFMのHard line breaks記法（行末の2つ以上の空白、またはバックスラッシュ）は無視される。

#### `emph`

強調（`*text*` または `_text_`）。子要素はインラインノード。

#### `strong`

太字強調（`**text**`）。子要素はインラインノード。

#### `strikethrough`

取り消し線（`~~text~~`）。子要素はインラインノード。

#### `underline`

下線（`__text__`）。子要素はインラインノード。

#### `highlight`

ハイライト（`==text==`）。子要素はインラインノード。

#### `superscript`

上付き文字（`^text^`）。子要素はインラインノード。

#### `subscript`

下付き文字（`~text~`）。子要素はインラインノード。

#### `spoilered_text`

スポイラー（`||text||`）。子要素はインラインノード。

#### `code`

インラインコード。

| 属性      | 型     | 説明         |
|-----------|--------|--------------|
| `literal` | string | コード内容   |

#### `link`

リンク。子要素はリンクテキスト（インラインノード）。

| 属性    | 型     | 説明           |
|---------|--------|----------------|
| `url`   | string | リンク先 URL   |
| `title` | string | リンクタイトル |

#### `embed`

Markdownの画像記法（`![alt](url)`）はすべてメディア埋め込み（画像・動画・音声・PDFなど）として解釈される。子要素は代替テキスト（インラインノード）。

| 属性     | 型     | 説明             |
|----------|--------|------------------|
| `url`    | string | メディアソース URL（`#` フラグメントを除いた部分） |
| `title`  | string | タイトル         |
| `props`  | object | URLフラグメント（`#key=value&flag`）から解析したキー・バリューのプロパティ。`=` のないフラグは `true` で表現。プロパティがない場合は省略。 |

#### `wikilink`

ウィキリンク。子要素はラベル（インラインノード）。ラベルが指定されていない場合、子要素は URL を `text` ノードとして持つ。

| 記法 | 説明 |
|------|------|
| `[[page]]` | ページへのリンク |
| `[[page\|label]]` | ラベル付きページリンク |
| `[[page#id]]` | ページ内ブロックへのリンク（`url` に `#id` フラグメントを含む） |
| `[[page#id\|label]]` | ラベル付きブロックリンク |

| 属性  | 型     | 説明         |
|-------|--------|--------------|
| `url` | string | リンク先 URL。ブロックリンクの場合は `#id` フラグメントを含む。 |

#### `footnote_reference`

脚注参照。

| 属性      | 型      | 説明                               |
|-----------|---------|------------------------------------|
| `name`    | string  | 脚注ラベル                         |
| `ref_num` | integer | 同一脚注への参照インデックス       |
| `ix`      | integer | 文書内での脚注インデックス         |

#### `shortcode`

絵文字ショートコード（例: `:rabbit:` → 🐰）。

| 属性    | 型     | 説明                                 |
|---------|--------|--------------------------------------|
| `code`  | string | ショートコード名（例: `"rabbit"`）   |
| `emoji` | string | 解決された絵文字（例: `"🐰"`）       |

#### `math`

コードスタイルの数式記法: インラインは `` $`formula`$ ``、ディスプレイ数式は `` $$`formula`$$ ``。`math_dollars`（`$$` 記法）は無効。

| 属性      | 型     | 説明     |
|-----------|--------|----------|
| `literal` | string | 数式内容 |

#### `html_inline`

生のインライン HTML。

| 属性    | 型     | 説明              |
|---------|--------|-------------------|
| `value` | string | 生の HTML 内容    |

#### `raw`

そのまま出力される内容。

| 属性    | 型     | 説明         |
|---------|--------|--------------|
| `value` | string | 生の内容     |

#### `escaped`

エスケープされた文字。

#### `escaped_tag`

エスケープされた HTML タグ（tagfilter による）。

| 属性    | 型     | 説明       |
|---------|--------|------------|
| `value` | string | タグ名     |

#### `hashtag`

ハッシュタグ（例: `#tag`, `#日記`）。AST後処理で `text` ノードから抽出される。

`#` は文字列先頭または半角スペース（U+0020）の直後に位置する必要がある。それ以外の文字の直後にある `#` はタグとして認識されない。タグ名はUnicode英数字、`_`、`-`、`/` で構成され、それ以外の文字（句読点、空白、文字列末尾など）でタグが終了する。`/` はタグ名の先頭および末尾に使用できない。

`\#` はリテラルの `#` として扱われ、ハッシュタグとして認識されない。

| 属性    | 型     | 説明                             |
|---------|--------|----------------------------------|
| `value` | string | `#` を除いたタグ名（例: `"日記"`） |

## 例

入力:

```markdown
## Hello **world**
```

出力:

```json
{
  "type": "document",
  "children": [
    {
      "type": "heading",
      "level": 2,
      "children": [
        { "type": "text", "value": "Hello " },
        {
          "type": "strong",
          "children": [
            { "type": "text", "value": "world" }
          ]
        }
      ]
    }
  ]
}
```
